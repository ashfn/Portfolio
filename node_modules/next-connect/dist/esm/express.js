export function expressWrapper(fn) {
    return (req, res, next) => {
        return new Promise((resolve, reject) => {
            fn(req, res, (err) => (err ? reject(err) : resolve()));
        }).then(next);
    };
}
