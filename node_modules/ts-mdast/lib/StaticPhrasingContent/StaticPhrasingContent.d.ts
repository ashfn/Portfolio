import { StaticPhrasingContent } from 'mdast';
import { Node } from 'unist';
/**
 * Type guard for StaticPhrasingContent.
 *
 * @param node - The node to test.
 * @returns True if node is StaticPhrasingContent.
 */
export declare function isStaticPhrasingContent(node: Node): node is StaticPhrasingContent;
/**
 * Type assertion for StaticPhrasingContent.
 *
 * @param node - The node to test.
 */
export declare function assertStaticPhrasingContent(node: Node): asserts node is StaticPhrasingContent;
