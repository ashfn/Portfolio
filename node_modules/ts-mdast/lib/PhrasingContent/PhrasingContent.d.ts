import { PhrasingContent } from 'mdast';
import { Node } from 'unist';
/**
 * Type guard for PhrasingContent.
 *
 * @param node - The node to test.
 * @returns True if node is PhrasingContent.
 */
export declare function isPhrasingContent(node: Node): node is PhrasingContent;
/**
 * Type assertion for PhrasingContent.
 *
 * @param node - The node to test.
 */
export declare function assertPhrasingContent(node: Node): asserts node is PhrasingContent;
