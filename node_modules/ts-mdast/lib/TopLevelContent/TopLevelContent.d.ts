import { TopLevelContent } from 'mdast';
import { Node } from 'unist';
/**
 * Type guard for TopLevelContent.
 *
 * @param node - The node to test.
 * @returns True if node is TopLevelContent.
 */
export declare function isTopLevelContent(node: Node): node is TopLevelContent;
/**
 * Type assertion for TopLevelContent.
 *
 * @param node - The node to test.
 */
export declare function assertTopLevelContent(node: Node): asserts node is TopLevelContent;
