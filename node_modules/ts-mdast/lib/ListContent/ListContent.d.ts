import { ListContent } from 'mdast';
import { Node } from 'unist';
/**
 * Type guard for ListContent.
 *
 * @param node - The node to test.
 * @returns True if node is ListContent.
 */
export declare function isListContent(node: Node): node is ListContent;
/**
 * Type assertion for ListContent.
 *
 * @param node - The node to test.
 */
export declare function assertListContent(node: Node): asserts node is ListContent;
