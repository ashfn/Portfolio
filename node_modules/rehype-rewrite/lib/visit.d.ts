import { Root, Parent, Content } from 'ts-mdast';
declare type Callback = (node: Content, index: number, parent: Parent | Content) => void;
export default function visit(tree: Root, callback: Callback): void;
export {};
