import { Root, Parent, Content } from 'ts-mdast';
import { Plugin } from 'unified';
export declare type MdastTransformer = (tree: Root) => void;
export declare type RehypeRewriteHandle = (node: Content, index: number, parent: Parent) => void;
declare const remarkRewrite: Plugin<[RehypeRewriteHandle?]>;
export default remarkRewrite;
